[project]
name = "temp"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "customtkinter>=6.0.0,<7",
    "ollama>=0.4.0",
    "openai>=1.0.0",
    "pillow>=10,<12",
    "pymupdf>=1.24.0",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]
